#include "binary_trees.h"

/**
 * binary_tree_is_heap - check if binary tree is heap
 * @tree: tree to check
 * Return: 1 if valid or 0 otherwise
 */

int binary_tree_is_heap(const binary_tree_t *tree)
{

	if (tree == NULL)
		return (0);
	return (1);
}
